## 記憶與任務管理行為

每次開始新任務時：
1. 使用 `get_memory_context` 搜尋相關過往經驗
2. 用 `auto_learn_project_conventions` 學習專案慣例
3. 完成工作後用 `auto_process_conversation` 自動提取記憶與任務

當需要規劃複雜功能時：
- 使用 `start_thinking_chain` 建立結構化思考流程
- 用 `decompose_task` 將大任務拆解成可執行步驟

定期使用 `get_project_summary` 回顧專案進度。